<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #111827;
      background-color: #f9fafb;
      margin: 0;
      padding: 0;
    }
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
    }
    .email-header {
      background: linear-gradient(135deg, #f59e0b 0%, #dc2626 100%);
      padding: 40px 24px;
      text-align: center;
    }
    .email-header h1 {
      color: #ffffff;
      font-size: 28px;
      font-weight: 700;
      margin: 0 0 8px 0;
    }
    .email-header p {
      color: #fef3c7;
      font-size: 16px;
      margin: 0;
    }
    .email-body {
      padding: 32px 24px;
    }
    .order-info {
      background-color: #fef3c7;
      border-left: 4px solid #f59e0b;
      border-radius: 8px;
      padding: 16px;
      margin-bottom: 24px;
    }
    .order-info p {
      margin: 4px 0;
      font-size: 14px;
      color: #92400e;
    }
    .order-info strong {
      color: #78350f;
    }
    .greeting {
      font-size: 16px;
      color: #374151;
      margin-bottom: 16px;
    }
    .section-title {
      font-size: 20px;
      font-weight: 600;
      color: #111827;
      margin: 24px 0 16px 0;
    }
    .order-items {
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      overflow: hidden;
      margin-bottom: 24px;
    }
    .order-item {
      display: table;
      width: 100%;
      border-bottom: 1px solid #e5e7eb;
      padding: 16px;
    }
    .order-item:last-child {
      border-bottom: none;
    }
    .item-details {
      padding: 8px 0;
    }
    .item-name {
      font-weight: 600;
      color: #111827;
      font-size: 16px;
      margin-bottom: 4px;
    }
    .item-qty {
      font-size: 14px;
      color: #6b7280;
    }
    .item-price {
      text-align: right;
      padding: 8px 0;
    }
    .item-total {
      font-weight: 600;
      color: #111827;
      font-size: 16px;
      margin-bottom: 4px;
    }
    .item-unit-price {
      font-size: 14px;
      color: #6b7280;
    }
    .customer-info {
      background-color: #f3f4f6;
      border-radius: 8px;
      padding: 16px;
      margin-bottom: 24px;
    }
    .customer-info p {
      margin: 4px 0;
      font-size: 14px;
      color: #4b5563;
    }
    .customer-info strong {
      color: #111827;
    }
    .summary-box {
      background-color: #f9fafb;
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 24px;
    }
    .summary-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      font-size: 16px;
      color: #111827;
    }
    .summary-label {
      font-weight: 500;
    }
    .summary-value {
      font-weight: 700;
      color: #dc2626;
    }
    .action-message {
      background-color: #fef2f2;
      border-left: 4px solid #dc2626;
      padding: 16px;
      border-radius: 4px;
      margin: 24px 0;
    }
    .action-message p {
      margin: 0;
      color: #991b1b;
      font-size: 14px;
    }
    .email-footer {
      background-color: #f3f4f6;
      padding: 24px;
      text-align: center;
      font-size: 12px;
      color: #6b7280;
    }
    .email-footer p {
      margin: 4px 0;
    }
    table {
      width: 100%;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- Header -->
    <div class="email-header">
      <h1>🔔 New Order Received!</h1>
      <p>Order #<%= @order.id %></p>
    </div>

    <!-- Body -->
    <div class="email-body">
      <!-- Greeting -->
      <p class="greeting">Hello <%= @admin.user.name.presence || @admin.user.email %>,</p>

      <!-- Alert Box -->
      <div class="order-info">
        <p><strong>⚡ Action Required:</strong> You have a new order that includes your products.</p>
        <p>Please prepare the following items for fulfillment.</p>
      </div>

      <!-- Order Items for This Admin -->
      <h2 class="section-title">Your Items to Fulfill</h2>
      <div class="order-items">
        <% @items_for_admin.each do |item| %>
          <table class="order-item">
            <tr>
              <td style="width: 70%;">
                <div class="item-details">
                  <div class="item-name"><%= item.product&.name %></div>
                  <div class="item-qty">Quantity: <%= item.quantity %></div>
                </div>
              </td>
              <td style="width: 30%;">
                <div class="item-price">
                  <div class="item-total"><%= number_to_currency(item.price * item.quantity) %></div>
                  <div class="item-unit-price"><%= number_to_currency(item.price) %> each</div>
                </div>
              </td>
            </tr>
          </table>
        <% end %>
      </div>

      <!-- Your Revenue -->
      <h2 class="section-title">Your Revenue from This Order</h2>
      <div class="summary-box">
        <% your_total = @items_for_admin.sum { |item| item.price * item.quantity } %>
        <div class="summary-row">
          <span class="summary-label">Total Revenue</span>
          <span class="summary-value"><%= number_to_currency(your_total) %></span>
        </div>
      </div>

      <!-- Customer Information -->
      <h2 class="section-title">Customer Details</h2>
      <div class="customer-info">
        <p><strong>Customer Name:</strong> <%= @order.customer&.user&.name.presence || "Not provided" %></p>
        <p><strong>Customer Email:</strong> <%= @order.customer&.user&.email %></p>
        <p><strong>Order Number:</strong> #<%= @order.id %></p>
        <p><strong>Order Date:</strong> <%= @order.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
        <p><strong>Full Order Total:</strong> <%= number_to_currency(@order.calculate_total) %></p>
      </div>

      <!-- Action Message -->
      <div class="action-message">
        <p><strong>📦 Next Steps:</strong></p>
        <p>Please log in to your admin dashboard to process this order and arrange shipping for your products.</p>
      </div>
    </div>

    <!-- Footer -->
    <div class="email-footer">
      <p>This is an automated notification from SaltShop.</p>
      <p>Please do not reply to this email.</p>
      <p>&copy; <%= Time.current.year %> SaltShop. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
