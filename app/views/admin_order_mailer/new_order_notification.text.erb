New order <%= @order.id %> placed

Hello <%= @admin.user.name.presence || @admin.user.email %>,

You have a new order that includes your products. Items you need to fulfill:

<% @items_for_admin.each do |item| %>
- <%= item.product&.name %> | Qty: <%= item.quantity %> | Unit: <%= number_to_currency(item.price) %> | Total: <%= number_to_currency(item.price * item.quantity) %>
<% end %>

Customer: <%= @order.customer&.user&.name.presence || @order.customer&.user&.email %>
Order total: <%= number_to_currency(@order.calculate_total) %>

Thank you.
