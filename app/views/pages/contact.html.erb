<% content_for :title, "Contact — SaltShop" %>
<% content_for :meta_description do %>
  Need help or have a wholesale inquiry? Contact SaltShop via email or the form below.
<% end %>

<section class="relative">
  <div class="max-w-3xl mx-auto px-6 lg:px-8 py-16">
    <h1 class="text-3xl sm:text-4xl font-semibold" style="color: var(--text);">Get in touch</h1>
    <p class="mt-3 text-muted">We usually reply within 1–2 business days. For urgent order issues, include your order number in the subject.</p>

    <div class="grid md:grid-cols-2 gap-8 mt-10">
      <div class="space-y-4">
        <div class="card p-5">
          <div class="flex items-center gap-3">
            <i data-lucide="mail" class="w-5 h-5 text-[var(--brand-700)]"></i>
            <div>
              <div class="font-semibold">Email</div>
              <a href="mailto:support@example.com" class="text-muted hover:text-[var(--brand-700)]">support@example.com</a>
            </div>
          </div>
        </div>
        <div class="card p-5">
          <div class="flex items-center gap-3">
            <i data-lucide="message-square" class="w-5 h-5 text-[var(--brand-700)]"></i>
            <div>
              <div class="font-semibold">Live chat</div>
              <p class="text-muted">Add your live chat link or tool here.</p>
            </div>
          </div>
        </div>
        <div class="card p-5">
          <div class="flex items-center gap-3">
            <i data-lucide="building-2" class="w-5 h-5 text-[var(--brand-700)]"></i>
            <div>
              <div class="font-semibold">Wholesale & PR</div>
              <p class="text-muted">Reach out via email for partnerships and press.</p>
            </div>
          </div>
        </div>
      </div>

      <div class="card p-6">
        <form action="#" method="post" class="space-y-4">
          <div>
            <label class="text-sm font-medium">Your name</label>
            <input type="text" class="mt-1 w-full p-2" placeholder="Jane Doe" />
          </div>
          <div>
            <label class="text-sm font-medium">Email</label>
            <input type="email" class="mt-1 w-full p-2" placeholder="you@example.com" />
          </div>
          <div>
            <label class="text-sm font-medium">Subject</label>
            <input type="text" class="mt-1 w-full p-2" placeholder="Order #12345 — Shipping" />
          </div>
          <div>
            <label class="text-sm font-medium">Message</label>
            <textarea rows="5" class="mt-1 w-full p-2" placeholder="How can we help?"></textarea>
          </div>
          <button type="submit" class="btn-primary"><i data-lucide="send" class="w-4 h-4"></i> Send message</button>
        </form>
      </div>
    </div>
  </div>
</section>
